import React from "react";
import styles from "./assets/scss/Header.scss";

function Header(props) {
  return <div id={styles.Header}>CSS in JS: Styled Component</div>;
}

export default Header;
